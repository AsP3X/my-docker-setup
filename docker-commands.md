# Docker commands

[docker]

```bash
[docker ps] - [docker ps -a]
Displays all running and stopped containers and their status.

[docker logs] - [docker logs -f]
Displays the logs of a container. and or follow the logs.

[docker exec] - [docker exec -it]
Execute a command in a container.

```